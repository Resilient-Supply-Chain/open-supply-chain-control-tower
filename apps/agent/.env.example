# AI Control Tower Environment Configuration (v2.0.0)
# Copy this file to .env and replace the dummy values below.

# --- Core RAG Configuration ---
# Optional. Controls parsing mode for policy RAG: LEGACY (local) or ADVANCED (LlamaParse).
# Impact: ADVANCED enables cleaner legal context; LEGACY uses local PDF parsing.
RAG_MODE=LEGACY #ADVANCED

# Dummy default. Required only if RAG_MODE=ADVANCED.
# Impact: Enables LlamaParse API calls for high-fidelity policy parsing.
LLAMA_CLOUD_API_KEY=dummy_llama_cloud_key

# --- LLM Providers (Optional unless you enable them) ---
# Dummy default. Required only if you run a PydanticAI model that targets Anthropic.
# Impact: Enables Claude-powered narrative synthesis.
ANTHROPIC_API_KEY=dummy_anthropic_key

# Dummy default. Required only if you switch to Hugging Face model endpoints.
# Impact: Enables Hugging Face-hosted model inference.
HUGGINGFACEHUB_API_TOKEN=dummy_hf_token
